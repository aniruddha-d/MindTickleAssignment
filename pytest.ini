[pytest]

addopts = -v -c pytest.ini

log_cli=false
log_level=INFO
log_file_format=%(asctime)s ::: %(pathname)s,%(lineno)d :::  %(levelname)s ::: %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

markers =
    user:           Mark testcases for users
    pet:           Mark testcases for pets
